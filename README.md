# Render Backend
Deploy as a Docker Web Service.
IMPORTANT: Leave Render's Docker Command EMPTY; do not put `docker build` there.
After deployment, copy the Render service URL into the Netlify frontend `app.js`.
